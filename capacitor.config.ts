import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.MyPosts.ionic.starter',
  appName: 'ionic-places',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
